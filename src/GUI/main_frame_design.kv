BoxLayout:
    orientation: 'vertical'

    BoxLayout:
        orientation: 'horizontal'
        size_hint_y: None
        height: '20dp'

        Button:
            id: 'archive_dropdown'
            text: 'Arquivo'
            size_hint: (None, None)
            size:(150,20)
            on_release: app.file_dropdown.open()

        Button:
            id: 'help_dropdown'
            text: 'Ajuda'
            size_hint: (None, None)
            size:(150,20)
            on_release: app.help_dropdown.open()

        Button:
            id: 'setup 'Configurações'
            text:'configurações'
            size_hint: (None, None)
            size:(150,20)
            on_release: app.settings_dropdown.open()
        Widget:
            size_hint_x: None
            width: 20

    BoxLayout:
        orientation: 'vertical'

        # Conteúdo principal do aplicativo
        Button:
            text: 'Botão 1'

        Button:
            text: 'Botão 2'

